export const incrementCount = () => ({ type: "ADD" });

// DECREMENT COUNTER BY 1
export const decrementCount = () => ({ type: "SUB" });

// RESET COUNTER
export const resetCount = () => ({ type: "RESET" });
